# Database Configuration
# For local development (running app outside Docker):
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/svc_users
# For Docker Compose (running app inside Docker):
# DATABASE_URL=postgresql://postgres:postgres@postgres:5432/svc_users

# Server Configuration
PORT=8001
HOST=0.0.0.0
ENVIRONMENT=development

# Security (optional for v1)
SECRET_KEY=your-secret-key-here-change-in-production

# JWT Configuration
JWT_ALGORITHM=RS256
JWT_ISSUER=https://api.example.com
JWT_AUDIENCE=https://api.example.com
JWT_EXPIRATION_DAYS=7
JWT_KID=auth-2025-10-15

# RSA Keys for JWT Signing (base64-encoded PEM format)
# Generate with: python generate_keys.py
# IMPORTANT: Keep JWT_PRIVATE_KEY secret - NEVER commit to version control
JWT_PRIVATE_KEY=your-base64-encoded-private-key-here
JWT_PUBLIC_KEY=your-base64-encoded-public-key-here

# External Services
# For Docker Compose (running app inside Docker - use host.docker.internal to reach host machine):
ORGANIZATIONS_SERVICE_URL=http://host.docker.internal:8000/api/v1
# For local development (running app outside Docker):
# ORGANIZATIONS_SERVICE_URL=http://localhost:8000/api/v1
